server:
  port:8090

spring:
  datasource:
    username: root
    password: 20010428Zty
    url: jdbc:mysql://localhost:3306/zty?characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCP

mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  config-location:  classpath:/mybatis/config/mybatis-config.xml


logging:
  level:
    root: debug